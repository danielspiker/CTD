let lista = ['a', 'b', 'c', 'd', 'e']
function imprimirInverso(array) {
  console.log(array.reverse())
}

imprimirInverso(lista)

let lista2 = [1, 2, 3, 4, 5]
function inverter(array) {
  let invertido = array.reverse()
  return invertido
}

console.log(inverter(lista2))
